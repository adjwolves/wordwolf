name: CI for frontend
on:
  push:
    branches:
      - main
    paths:
      - "wordwolf-frontend/**"
      - ".github/workflows/ci-frontend.yml"
      - "!**.md"
  pull_request:
    branches:
      - "*"
    paths:
      - "wordwolf-frontend/**"
      - ".github/workflows/ci-frontend.yml"
      - "!**.md"

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
