name: CI for frontend
on:
  push:
    branches:
      - main
    paths:
      - "woldwolf-frontend/**"
      - "!**.md"
  pull_request:
    branches:
      - "*"
    paths:
      - "woldwolf-frontend/**"
      - "!**.md"

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
